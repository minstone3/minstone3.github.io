export type ValueItem = {
  number: string
  title: string
  description: string
}

export type SolutionItem = {
  mark: string
  title: string
  description: string
}

export type ArchitectureLayer = {
  title: string
  note: string
}

export type IndustryItem = {
  number: string
  title: string
  description: string
}
